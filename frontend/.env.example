# Production (Vercel): optional if vercel.json proxies /api to Railway.
# Required for local dev when you want the dashboard to read Railway data:
# VITE_API_URL=https://kavina-expense-tracker.up.railway.app
